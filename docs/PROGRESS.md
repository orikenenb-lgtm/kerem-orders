# PROGRESS.md — יומן חי של שדרוג 2026

## Wave 0 — מיפוי (2026-07-23) ✅
- הוקם ענף `feat/kerem-upgrade-2026`; גיבוי מצב קיים בענף `backup/pre-upgrade-2026` (דחיפת tag נחסמה ע"י ה-proxy של הסביבה — ענף גיבוי שקול ובטוח יותר).
- נכתב `backups/schema-before.sql` — צילום סכמה, RLS, פונקציות, cron.
- שני Mapper agents מיפו frontend + data layer; הדו"חות אוחדו ל-`docs/AUDIT.md`.
- **ממצאים מרכזיים:** אין 3D בכלל (ה-hero הוא 12MB פריימים placeholder); תמונות מרווחית דרך פרוקסי (אין EXIF handling); סנכרון קיים ורץ יומית; מינימום הזמנה חי = ₪500 (הפרומט מבקש 3,500 — ממתין להחלטה); מינימום ומחירים נאכפים client-only; הסנכרון דורס עריכות אדמין על עמודות מסונכרנות (עמודות חדשות בטוחות).
- קבצים שנוצרו: `docs/AUDIT.md`, `docs/PROGRESS.md`, `docs/DECISIONS.md`, `backups/schema-before.sql`, `landing/lib/featureFlags.ts`.
- נשבר: כלום. לא שונה קוד קיים.

אושר ע"י הבעלים ("אני מאשר, תמשיך") + הנחיה להריץ את כל הגלים ברצף. הדלקת דגלים עדיין תחכה לאישור סופי.

## Wave 1 — מסך פתיחה (2026-07-23) ✅
- החלטות בעלים: מינימום הזמנה עודכן ל-3,500 ₪ (חי ב-DB!); טלפון 050-852-4448 מאושר לפרסום.
- נבנה `LandingV2` מאחורי `ff_new_landing` (כבוי): מסך נקי לפי המפרט — כותרת גרדיאנט, "כניסה לחשבון" / "פתיחת חשבון לקוח", קופסת מינימום+מע"מ (נקרא חי מה-DB), לינק שקט לקטלוג הציבורי, טלפון עם חיוג בלחיצה, בלי אנימציות.
- שינויים חיים שאושרו: "חשבון סיטונאי"→"חשבון לקוח" בכל טקסט פתיחת-חשבון; ה-₪500 הקשיח בדף הבית → נבנה מ-MIN_ORDER_FALLBACK.
- שער איכות: Reviewer מצא 2 כשלי ניגודיות (כפתור 3.33:1, פוטר 3.19:1) — תוקנו (#CC4A00→#C81E56, #6B6357); UX-Simplifier דרש לינק קטלוג + "כניסה לחשבון" + תווית לטלפון — יושם. build+tsc ירוקים. flag-off = זהות מלאה (אומת ע"י Reviewer ברמת HTML).
- קבצים: LandingV2.tsx, page.tsx, config.ts, NumbersSection, ClosingCTA, login, register.
- נשבר: כלום.

## Wave 2 — יישור תמונות (2026-07-23) ✅ (חלקו נדחף ל-main כתיקון חי)
- אבחון (docs/IMAGE_AUDIT.md): ~50% מהתמונות עם EXIF Orientation=6 — הוגשו שוכבות מאז שהוספנו את צינור ההקטנה. באג חי!
- `rivhit-img` v7: יישור אוטומטי לפי EXIF לפני ההקטנה; מצבי `meta=1`/`dims=1` לאבחון. כיוון סיבוב אומת אמפירית + E2E (3264×2448 or=6 → 480×640).
- `rivhitImg()` עם `v=2` — עקיפת המטמון השגוי. **נדחף ישירות ל-main (PR #35) כתיקון באג חי**, לא הוחזק בענף.
- מניעה קדימה: כל תמונה חדשה עוברת באותו צינור. WebP נדחה במודע (ראו IMAGE_AUDIT).
- נשבר: כלום. קבצי המקור ברווחית לא נגעו.
