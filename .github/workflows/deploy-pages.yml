name: Deploy landing page to GitHub Pages

on:
  push:
    branches:
      - main
      - claude/kerem-toys-landing-fso0gm
    paths:
      - "landing/**"
      - "docs/**"
      - ".github/workflows/deploy-pages.yml"
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: landing
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
      - run: npm ci
      - run: npm run build
      # Publish the CRPS presentation alongside the landing page at /crps/.
      # The landing page output is left untouched.
      - name: Add CRPS presentation at /crps
        run: |
          mkdir -p out/crps
          cp ../docs/index.html out/crps/index.html
      - uses: actions/upload-pages-artifact@v3
        with:
          path: landing/out

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
